int main ( ) {
    int vetor;
    vetor = {5,2,7,6,10}   /*Cria um vetor para teste*/
    heapSort(vetor, 5)
}

int heapSort (vetor, qtdElementos) {
    tipoHeap heap
    heap = insere elementos do vetor um a um no heap
    while (heap != vazio) {
      vetorOrdenado[] = removeRaizHeap(heap)
    }
    return vetorOrdenado
} 